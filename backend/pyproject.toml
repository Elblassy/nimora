[project]
name = "qissati-backend"
version = "1.0.0"
description = "Qissati - Interactive AI Storybook Backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
