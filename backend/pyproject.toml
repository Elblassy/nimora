[project]
name = "nimora-backend"
version = "1.0.0"
description = "Nimora - Interactive AI Storybook Backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
